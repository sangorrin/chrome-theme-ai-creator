# OpenAI API key (get from https://platform.openai.com/api-keys)
OPENAI_API_KEY=your_openai_api_key_here

# Model for text generation (theme names, colors, descriptions)
# Options: gpt-4o, gpt-4-turbo, gpt-4, gpt-3.5-turbo
TEXT_MODEL=gpt-4o

# Model for image generation (NTP background images)
# Options: dall-e-3, dall-e-2
IMAGE_MODEL=dall-e-3

# Base URL for your API endpoints (http://localhost:3000 for local dev)
NUXT_PUBLIC_API_URL=http://localhost:3000

# URL where generated theme files are stored (Vercel Blob Storage or similar)
NUXT_PUBLIC_THEME_STORAGE_URL=https://your_storage_url_here